syntax = "proto3";
package neutronorg.neutron.tokendistribution;

import "gogoproto/gogo.proto";

option go_package = "github.com/neutron-org/neutron/x/tokendistribution/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}
